apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-backend-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: java-backend-service
            port:
              number: 8080
